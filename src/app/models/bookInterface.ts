export interface BookInterface {
    id: string,
    image: string[],
    title: string,
    fisrtPublished: number,
    author: string[],
    editionCount: string,
    PagesNo: string,
    isLoved: boolean
}